apiVersion: v1
type: PersistentVolume
metadata:
  name: pv-vol1
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath: # Not to be used in Production environment.
    path: /tmp/data
